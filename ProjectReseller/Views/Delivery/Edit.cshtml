@model ProjectReseller.Models.delivery

@{
    ViewBag.Title = "Edit";
}

<h2>Opcje</h2>


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h3>Zmień status zamówienia</h3>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.id)

        @Html.HiddenFor(model => model.users_id)

        @Html.HiddenFor(model => model.item_category_id)

        @Html.HiddenFor(model => model.item_users_id)

        @Html.HiddenFor(model => model.item_id)

        @Html.HiddenFor(model => model.delivery_status_id)

        <hr />

        <h4>@Html.DisplayFor(model => model.item.name)</h4>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
